"""fast-mcp-jobvite: an MCP server for the Jobvite ATS.

U0 creates this package so the manifest has something to build and tests
have something to import. Every module DESIGN.md:280-293 lists is owned
by a later unit; none of them is written here.
"""

__version__ = "0.1.0"
