# U14 - argument-layer hardening: the completeness sweep

**Read `docs/briefs/PREAMBLE.md` first.** Tools, isolation, evidence standards, gates and delivery
rules are there and are not repeated here.

Your agent name is `u14-arguments`. Your branch is `feat/u14-arguments`. Your report goes to
`docs/worklogs/U14-IMPL-REPORT.md`, committed on your branch.

## Why this unit ran last, and what that means for you

`docs/plans/IMPLEMENTATION-PLAN.md` §U14 (line 1362), in its own words:

> **U14 is the sweep proving the inbound set is COMPLETE**, so starting it before all three have
> landed sweeps an incomplete set and passes: **a vacuous green on the unit whose entire job is
> completeness.**

The plan named U5, U8 and U12. **U10 landed after that sentence was written and adds a fourth input
model - `create_candidate`'s.** All four are now in. **Your sweep is only worth something if it
enumerates the CONTAINER rather than a list somebody typed**: find every input model by walking the
tool modules, not by naming four of them, and assert the two sets are equal. A hand-kept list of
input models beside the models themselves is the defect this repository has recorded nine times.

## Read the canon BEFORE you touch anything

**The design is FROZEN at `c15b138`** - `git show c15b138:docs/DESIGN.md`, never the working tree.

1. `docs/plans/IMPLEMENTATION-PLAN.md` §U14 (1362) **and §1**
2. `git show c15b138:docs/DESIGN.md` - **§2.1**, `181-190`, `1370-1371`, and §8 cases **#7, #8, #9**
3. **Every** ADR in `docs/adr/` - there are 28 now, and ADR-0012 governs where constraints live
4. `CONTRIBUTING.md`, `docs/OBLIGATIONS.md`
5. `src/fast_mcp_jobvite/utils/constraints.py` - the shared rule every input model reuses
6. The four input models: `tools/jobs.py` (two), `tools/candidates.py` (two)

## The work

The three inbound controls of §2.1, **all in the input models**:

- `strict=True` with extra keys forbidden; explicit `max_length` and identifier regexes.
- **UTF-8 validation rejecting C0/C1 control characters other than tab, newline and carriage return,
  and Unicode bidi overrides.**
- The four structural limits: **depth 5, 1,000 list items, 100 dict keys, 1 MiB body.**

## THE TRAP A NAIVE UNIT FALLS INTO, and the plan states it outright

**`DESIGN.md:181-190`: none of these rejections carries a problem object.** They live in the input
models, run **before the tool body**, and are raised by the framework.

> An earlier design revision said 400; the registry says 422; **and neither reaches the caller on
> this path at all.**

**Assert FAIL-CLOSED BEHAVIOUR, not a problem-object shape.** The 422 row in the registry is not dead
- it serves validation detected *inside* the tool body - but it is unreachable pre-dispatch. A test
asserting a problem shape here is asserting something the code cannot produce.

## THE BLANKET POSITIVE CONTROL, on all three cases

`DESIGN.md:1370-1371` requires it and an earlier draft stated it for only one of the three:

- **#7**: a well-formed argument **passes** schema validation.
- **#8**: an ordinary name **passes** the control-character check.
- **#9**: a payload sitting **JUST INSIDE each of the four structural limits is ACCEPTED.**

> **A limit test with no accepting arm cannot tell a correct limit from a rejector.**

Four limits means four accepting arms, not one.

## The sweep that has been passing over an empty corpus since U2

**Re-run U2's repository-wide no-`success`-envelope rule across the completed tool set.** U2 owns
that rule and asserts it where it lives - but at U2-completion the repository held almost no code, so
**the assertion passed over an empty corpus and has kept passing regardless of what every later unit
did.** You are the unit where it acquires teeth. Prove your version can fail before you trust it.

## Standing requirements

- **Every behaviour gets an amputation or mutation PROVED able to fail** - write the control, run it
  against the UNFIXED code, watch it survive, then fix, then watch it die.
- New harnesses in `scripts/check-u14-arguments-{controls,amputation}.sh`, wired through
  `scripts/ci-harness-gate.sh`.
- **ANCHOR COLLISIONS ARE NOW ROUTINE, not a hazard.** U12 broke eight of U5's anchors and U10 broke
  one of U8's twice - once on a set gaining a member, once when `ruff format` re-wrapped it. You are
  editing four input models that already look alike. Run
  `python3 scripts/check-harness-anchors.py --self-check` early and often, and when an anchor stops
  being unique **WIDEN it with a line unique to its own subject, never shorten the other.**
- **Run `ruff format` BEFORE your final harness run**, then re-run the harnesses AND the anchor
  checker - that is what caught U10's second collision.
- **A MUTATION HARNESS OWNS THE WORKING TREE FOR ITS WHOLE RUN.** Restore with `cp`, verify with
  `cmp`. **Never `git stash`, never `git checkout <path>`.**
- `docs/DESIGN.md` is FROZEN. A defect there is a **Proposed** ADR - **ADR-0029 is the next free
  number, and `docs/reviews/check-adr-numbers.py` now refuses a duplicate.** Two agents claimed one
  number today, an hour apart, both correct when they looked.
- Cite by subject. `check-design-citation-shape.py`, `check-standards-citations.py`,
  `check-settings-are-read.py` and `check-adr-numbers.py` all exit 0 today and all are wired.
- **Both floors are DERIVED, never retyped.** Report them; **the `ci.yml` edits are MINE.**
- **No `Co-Authored-By:` or "Generated with" trailer. Ever.**

## Isolation

You own `utils/constraints.py`, the INPUT MODELS in `tools/jobs.py` and `tools/candidates.py`, your
new harnesses and test modules.

**`code-review-r7` is live and READ-ONLY**, reviewing the four units you are sweeping. It will not
touch your files. **Do not restructure the tool bodies** - your subject is the argument layer, and a
reviewer reading a merged SHA should still recognise what it read.

## How to deliver

Commit and push your branch. **Do NOT merge to main and do NOT push main.** `SendMessage` to
`"team-lead"` as your FINAL action: what you built, gate exit codes read from the terminal, the new
floors, every harness row and whether it fired, **how you enumerated the input models rather than
listing them**, whether each of the four structural limits has its own accepting arm, and **what you
could not settle**.
