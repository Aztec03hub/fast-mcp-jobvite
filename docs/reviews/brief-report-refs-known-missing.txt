# Reports a brief cites that are not tracked in this repository.
#
# One line per report:  <basename>  <reason>
#
# Recording a line is NOT a waiver. It says the state is known and
# accepted, and that nobody should spend another hour looking.
#
# THE RECORD MUST SHRINK. A line whose report becomes tracked is an
# ERROR, and so is a line nothing cites any more. An excuse list that
# only grows stops being read.
#
# Gate: docs/reviews/check-brief-report-references.py

WORKLOG-187-floor-container.md  IN FLIGHT, not missing: `suborch-187` is running and this is the worklog its brief tells it to write. This line is designed to EXPIRE - the moment that worklog is committed, the checker fails with "a recorded entry now resolves" and the line must be deleted. That is the record deleting itself rather than accumulating.
REVIEW-R20.md  IN FLIGHT, same shape: `review-r20` is dispatched and its brief names this report. Delete this line when the report lands. Two in-flight entries at once is what this record is for - the alternative is a gate that goes red every time an agent is dispatched, which is a gate that gets switched off.
